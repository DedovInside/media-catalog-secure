# Игнорируемые правила (с объяснением почему)
ignored:
  # DL3008: Pin versions in apt-get install
  # Игнорируем, т.к. используем --no-install-recommends и фиксированный базовый образ
  - DL3008
  
  # DL3018: Pin versions in apk add
  # Не применимо - мы используем Debian (apt), а не Alpine (apk)
  - DL3018

# Правила, которые мы НЕ игнорируем (важные для безопасности):
# - DL3000: Use absolute WORKDIR
# - DL3001: For some bash commands it makes no sense running them in a Docker container
# - DL3002: Last USER should not be root
# - DL3003: Use WORKDIR to switch to a directory
# - DL3004: Do not use sudo
# - DL3006: Always tag the version of an image explicitly
# - DL3007: Using latest is prone to errors
# - DL3009: Delete the apt-get lists after installing something
# - DL3015: Avoid additional packages by specifying --no-install-recommends
# - DL3025: Use JSON notation for CMD and ENTRYPOINT
# - DL3045: COPY to a relative destination without WORKDIR set

# Уровень строгости (error, warning, info, style)
failure-threshold: warning

# Формат вывода для CI
# format: json  # Будем указывать в команде запуска